Phosphor icon glyph — use for every icon in Oper Radar UI; never hand-draw SVG icons.
```jsx
<Icon name="truck" /> <Icon name="house" weight="fill" size={20} />
```
- weight: `regular` (default, inline), `fill` (nav, active, icon-in-circle), `bold` (≤12px).
- Browse names at phosphoricons.com.
