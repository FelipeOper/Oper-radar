Brand logo (raster, from the official files) — use in sidebar, login, reports.
```jsx
<Logo base="../assets/" height={24} /> <Logo variant="mark" theme="light" height={40} />
```
- Never recolor or redraw. Dark theme → `theme="dark"`; white backgrounds → `theme="light"`.
